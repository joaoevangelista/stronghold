<% if a.trackable %>

<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-xs-12">
        <p>
          <span><i class="fa fa-2x fa-pencil-square-o"></i></span>
          &nbsp;&nbsp;
          <!--<%= image_tag gravatar(a.trackable.user.email, 40), height: 40, class: 'img-circle' %>-->
          <%= mail_to a.trackable.user.email, a.trackable.user.name %>
          <span>updated</span>
          <%= link_to a.trackable.title, announcement_path(a.trackable) %>
          <span>announcement.</span>
        </p>
      </div>
    </div>
  </div>
</div>
<% end %>

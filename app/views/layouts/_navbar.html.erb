<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= image_tag('fortress.svg', height: 40, class: 'header-logo')  %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <% if user_signed_in? %>
      <ul class="nav navbar-nav">
        <li><%= link_to icon('heartbeat','Activity', class: 'fa-fw'), activities_path %></li>
        <li><%= link_to icon('paper-plane-o', 'Announcements', class: 'fa-fw'), announcements_path %></li>
        <li><%= link_to icon('calendar-check-o', 'Events', class: 'fa-fw'), events_path %></li>
        <li><%= link_to icon('database','Issues', class: 'fa-fw'), issues_path %></li>
      </ul>
      <% end %>
    </li>
  </ul>
  <ul class="nav navbar-nav navbar-right">
    <% if current_user %>
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
      aria-haspopup="true" aria-expanded="false"><%= current_user.name %><span class="caret"></span></a>
      <ul class="dropdown-menu">
        <li><%= link_to icon('pencil', 'Edit Account', class: 'fa-fw'), edit_user_registration_path(current_user) %></li>
        <li class="divider"></li>

        <li><%= link_to(icon('sign-out', 'Sign Out', class: 'fa-fw'), destroy_user_session_path, method: :delete) %></li>
      </ul>
    </li>
    <% else %>
    <li>
      <%= link_to icon('sign-in', 'Sign In', class: 'fa-fw'), new_user_session_path, class: 'nav-link' %>
    </li>
    <% end %>
  </ul>
</div>
</div>
</nav>

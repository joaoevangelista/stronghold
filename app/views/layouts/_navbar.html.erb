<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= image_tag('fortress.svg', height: 40, class: 'header-logo')  %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <% if user_signed_in? %>
      <ul class="nav navbar-nav">
        <li><%= link_to icon('bullhorn', t('announcement.plural'), class: 'fa-fw'), announcements_path %></li>
        <li><%= link_to icon('calendar-check-o', t('event.plural'), class: 'fa-fw'), events_path %></li>
        <li><%= link_to icon('cubes',t('issue.plural'), class: 'fa-fw'), issues_path %></li>
      </ul>
      <% end %>
    </li>
  </ul>
  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
    <li><%= link_to icon('heartbeat',t('activity.singular'), class: 'fa-fw'), activities_path %></li>
    <%= render 'layouts/profile_menu' %>
    <% else %>
    <li>
      <%= link_to icon('sign-in', t('user.sign_in'), class: 'fa-fw'), new_user_session_path, class: 'nav-link' %>
    </li>
    <% end %>
  </ul>
</div>
</div>
</nav>
